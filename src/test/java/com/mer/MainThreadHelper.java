package com.mer;

import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;

import com.test.Login;



public class MainThreadHelper {
	
 /*	public static ThreadLocal<Login> tLogin = new ThreadLocal<>();
	public static ThreadLocal<Home> tHome = new ThreadLocal<>();
	public static ThreadLocal<Utility> tUtility = new ThreadLocal<>();
	
	
	@BeforeMethod(alwaysRun = true)
	public void init() {
		
		Login login = new Login();
		Home home = new Home();
		Utility utility = new Utility();
						
		MainThreadHelper.tLogin.set(login);     
		MainThreadHelper.tHome.set(home);
		MainThreadHelper.tUtility.set(utility);  
	
						
	}
	
	
	@AfterMethod(alwaysRun = true)
	public void cleanUp() {
		tLogin.remove();
		tHome.remove();
		tUtility.remove();
		
		
	} */
	
	

}
